<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aplicativo304">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".kabum"></activity>
        <activity android:name=".mercadolivre" />
        <activity android:name=".submarino" />
        <activity android:name=".magalu" />
        <activity android:name=".zoom" />
        <activity android:name=".Sobre" />
        <activity android:name=".tela5" />
        <activity android:name=".tela4" />
        <activity android:name=".Tela3" />
        <activity android:name=".Tela2" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>